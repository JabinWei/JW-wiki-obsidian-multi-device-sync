---
type: framework
style: sketch-notes
aspect: 4:3
---

Illustration: Python LEGB scope hierarchy — nested box diagram.

STYLE: Sketch-notes educational diagram. Warm cream paper, hand-drawn lines, soft pastel fills.

CONTENT: Four concentric nested boxes showing LEGB scope layers, outermost to innermost:

- Built-in (largest box, pastel gray): open, range, print, len — "Python 内置"
- Global (next box, pastel blue): module-level variables, global keyword — "全局变量"
- Enclosing (next box, pastel green): outer function variables, nonlocal keyword — "外层函数"
- Local (innermost box, pastel coral): function parameters, local vars — "局部变量"

A search arrow from Local → outward shows how Python looks up variables. Title "LEGB 作用域查找规则" in hand-drawn text.

COLORS: Cream paper (#FAF5ED). Black ink lines. Pastel fills. Title in charcoal.

Watermark: "@Jabin W." bottom-right.
