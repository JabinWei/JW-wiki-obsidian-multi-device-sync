---
type: framework
style: sketch-notes
aspect: 1:1
---

Illustration: Python built-in functions family tree mind-map.

STYLE: Sketch-notes educational framework. Warm cream paper, black hand-drawn lines, soft pastel color blocks. Clean hand-drawn feel.

CONTENT: A radial mind-map with a central rounded node "Built-in Functions" in deep charcoal. Six branches radiate outward, each in a different pastel color block:

1. Math & Numbers (pastel blue): `abs()` `round()` `sum()` `pow()`
2. Type Conversion (pastel green): `int()` `str()` `list()` `bool()`
3. Iterators & Sequence (pastel coral): `len()` `range()` `zip()` `enumerate()`
4. Object & Reflection (pastel purple): `type()` `isinstance()` `getattr()` `dir()`
5. Input/Output (pastel yellow): `print()` `input()` `open()` `help()`
6. Others (pastel teal): `eval()` `exec()` `breakpoint()` `callable()`

Each branch: category name in bold hand-drawn text, then 3-4 function names in smaller text below. Branches connected by hand-drawn curved lines from center.

COLORS: Cream paper (#FAF5ED). Black ink lines. Six distinct pastel branch colors. Center node in deep navy (#1E3A5F).

Watermark: "@Jabin W." bottom-right.
