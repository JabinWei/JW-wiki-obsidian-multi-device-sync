---
cssclasses:
aliases:
tags:
  - 1：用
  - 2：混淆
  - 3：期望集合转换后保持顺序
---
$$
\documentclass[border=5mm]{standalone} \usepackage{pgfplots} \pgfplotsset{compat=1.18} \begin{document} \begin{tikzpicture} \begin{axis}[ width=10cm, % 图像宽度 height=7cm, % 图像高度 axis lines=middle, % 坐标轴居中显示（十字形） xlabel={$x$}, % x轴标签 ylabel={$y$}, % y轴标签 xmin=-3, xmax=3, % x轴范围 ymin=-1, ymax=9, % y轴范围 xtick={-3,-2,...,3}, % x轴刻度 ytick={0,2,...,8}, % y轴刻度 domain=-3:3, % 函数定义域 samples=100, % 采样点数量（越多越平滑） grid=major, % 显示主网格线 grid style={dashed, gray!50}, % 网格样式 title={$y = x^2$} % 图像标题 ] % 绘制 y = x^2 的曲线 \addplot[ blue, % 曲线颜色 thick, % 线条粗细 smooth % 平滑处理 ] {x^2}; % 可选：添加一个标注点 \node[above right, font=\small] at (axis cs:2,4) {$(2, 4)$}; \end{axis} \end{tikzpicture} \end{document}
$$